# creditcard-statement-service
Credit Card Management - Statement & Transaction Service
## Build: `mvn spring-boot:run` or `docker build -t creditcard-statement-service . && docker run -p 8083:8080 creditcard-statement-service`
## API: http://localhost:8083/swagger-ui.html
